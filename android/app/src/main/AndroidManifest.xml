<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- The app is a persistent watch-trigger listener that must survive Doze; this lets it ask
         the user directly to exempt it from battery optimization. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PebbleRecorder"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PebbleRecorder">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Bound by the Pebble/Core companion app whenever the PebbleRecorder watchapp
          (see watch/package.json for its UUID) is opened on a paired watch. Must stay
          registered under this exact action for PebbleKit Android 2 to find it.

          Also self-started (specialUse type) by MainActivity as soon as the app is opened, and
          kept running persistently as a low-priority "armed" foreground service from then on.
          Android forbids starting a *new* microphone-type foreground service while the app has
          no visible UI (see MainActivity's start call) - but adding the microphone type to an
          *already-foregrounded* service is allowed, so recording only works because this service
          is already in the foreground by the time a watch COMMAND arrives.
        -->
        <service
            android:name=".PebbleListenerService"
            android:exported="true"
            android:foregroundServiceType="specialUse|microphone">
            <intent-filter>
                <action android:name="io.rebble.pebblekit2.RECEIVE_DATA_FROM_WATCH" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="pebble_watch_trigger_listener" />
        </service>

        <!-- Re-arms PebbleListenerService after a reboot - see BootCompletedReceiver.kt. -->
        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- exported=true: the launcher/home-screen process needs to reach this one to bind and
             update the widget; nothing else here is exported for that reason. -->
        <receiver
            android:name=".widget.RecordingStatusWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recording_status_widget_info" />
        </receiver>

        <!--
          Exposes the bundled watch.pbw (copied into assets/ from watch/build/ at build time, see
          app/build.gradle.kts) so the "Install watchapp on watch" button can hand it to whatever
          app registered to open *.pbw files - the Pebble/Core companion app - via a content://
          URI, without needing a raw file:// URI (blocked by StrictMode on modern Android).
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
